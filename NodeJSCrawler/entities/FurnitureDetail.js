export default class FurnitureDetail {
  constructor(name, latestPrice, descriptions, images) {
    this.name = name;
    this.descriptions = descriptions;
    this.latestPrice = latestPrice;
    this.images = images;
  }   
}