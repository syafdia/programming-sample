export default class FurniturePreview {
  constructor(productId, name, latestPrice, detailUrl) {
    this.productId = productId;
    this.name = name;
    this.latestPrice = latestPrice;
    this.detailUrl = detailUrl;
  }   
}